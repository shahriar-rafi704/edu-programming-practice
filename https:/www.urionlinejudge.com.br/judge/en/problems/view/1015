#include<stdio.h>
#include<math.h>
int main()
{
  double x1,x2,y1,y2,xsum,sxsum,ysum,sysum,sum,res;
  scanf("%lf %lf",&x1,&y1);
  scanf("%lf %lf",&x2,&y2);
  xsum=(x2-x1)*(x2-x1);
  ysum=(y2-y1)*(y2-y1);
  sum=xsum+ysum;
  res=sqrt(sum);

  printf("%.4lf", res);

  return 0;
}
